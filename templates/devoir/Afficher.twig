{% extends "base.html.twig" %}

{% block title %}AgendSup - Liste des devoirs{% endblock %}


{% block body %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        var calendarEl = document.getElementById('calendar');
        var calendar = new FullCalendar.Calendar(calendarEl, {
            initialView: 'timeGridWeek',
            slotDuration: '00:15', 
            slotMinTime: '08:00:00',
            slotMaxTime: '19:00:00',
            nowIndicator: true,
            contentHeight: 450,
            locale: 'fr',
            backgoundColor: '#808080',
            headerToolbar: {
                left: '',
                center: 'title prev next today',
                right: ''
            },
             
            events: 'index.php?controleur=devoir&methode=api_events',

            eventDidMount: function (info) {
            const description = info.event.extendedProps.description;
            if (!description) return;

                new bootstrap.Popover(info.el, {
                title: info.event.title,
                content: description,
                trigger: 'hover focus',
                placement: 'top',
                html: true,
                container: 'body'
                 });
            },

            eventSourceFailure: function() {
            alert("Erreur : Impossible de charger les données depuis l'API.");
            },
            // Optionnel : s'assurer que les dates s'affichent même si l'heure est absente
            displayEventTime: true,
            eventTextColor: 'white',
        });
        
        calendar.render();
    });
</script>

{% if session.user is defined and session.user.role == 'delegue' %}
    <div class="mb-3 text-end">
        <a href="index.php?controleur=devoir&methode=creer" class="btn btn-primary">
            <i class="fas fa-plus"></i> ➕ Ajouter un devoir
        </a>
    </div>
{% endif %}


<div class="container mt-4">
    <div id='calendar'></div>
</div>




{% endblock %}